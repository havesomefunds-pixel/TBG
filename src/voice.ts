import { prisma } from './database.js';
export async function startVoice(guildId: string, userId: string, channelId: string, now = new Date()) { return prisma.voiceSession.upsert({ where: { guildId_userId: { guildId, userId } }, create: { guildId, userId, channelId, startedAt: now, lastAccruedAt: now }, update: { channelId, lastAccruedAt: now } }); }
export async function stopVoice(guildId: string, userId: string, now = new Date()) { return prisma.$transaction(async (tx) => { const session = await tx.voiceSession.findUnique({ where: { guildId_userId: { guildId, userId } } }); if (!session) return 0; const seconds = Math.max(0, Math.floor((now.getTime() - session.lastAccruedAt.getTime()) / 1000)); await tx.voiceSession.delete({ where: { id: session.id } }); const member = await tx.member.upsert({ where: { guildId_userId: { guildId, userId } }, create: { guildId, userId, voiceSeconds: BigInt(seconds) }, update: { voiceSeconds: { increment: BigInt(seconds) } } }); return Number(member.voiceSeconds); }); }
